def is_odd(number):
    "Returns True if the number is odd, False otherwise."
    return number % 2 != 0


def divide(number, number2):
    "Retuns the result of a division."
    return number / number2
